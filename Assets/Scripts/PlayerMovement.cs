using UnityEngine;

// Kept only so existing Player components do not become missing scripts.
// Battle actions are now driven by PlayerBattleActions and UI buttons.
public class PlayerMovement : MonoBehaviour
{
}
